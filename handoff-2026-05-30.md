# Handoff — "Say It Right" VS Code extension (next: v0.3.0 full rebrand + voice/model selection)

- Date: 2026-05-30
- Repo: `/Users/xianweizhang/Projects/vscode-english-coach` · branch `main` · pushed to `github.com/xwzhangSZU/vscode-english-coach` · latest commit `2848035`
- User: publishes to VS Code Marketplace; `VSCE_PAT` is exported in the shell env (verify with `npx @vscode/vsce verify-pat xianwei-zhang`, never echo it).

This doc references existing artifacts instead of duplicating them. Read those first:
- Design spec: `docs/superpowers/specs/2026-05-30-say-it-right-vscode-design.md`
- Plans: `docs/superpowers/plans/2026-05-30-say-it-right-vscode-mvp.md` (Plan 1, DONE), `…-word-highlight.md` (Plan 2, future), `…-say-it-right-recorder.md` (Plan 3, future)
- Stave style mockups: `docs/superpowers/mockups/2026-05-30-stave-variants.html` (user chose **Style 1**)
- Memory (Marketplace gotchas + publishing): `~/.claude/projects/-Users-xianweizhang-Projects-vscode-english-coach/memory/publishing.md` and `say-it-right.md`

---

## 1. What's LIVE right now
- **`xianwei-zhang.vscode-english-coach` v0.2.2** — Marketplace, displayName **"Say It Right 英语口语教练"**. https://marketplace.visualstudio.com/items?itemName=xianwei-zhang.vscode-english-coach . Also installed on the user's local VS Code (`code --list-extensions --show-versions | grep english-coach` → 0.2.2).
- **`xianwei-zhang.vscode-skill-curator` v0.1.11** — a *separate* extension (skill manager), moved this session from publisher `XianweiZhang` → `xianwei-zhang` (had to rename `name` + `displayName` due to the reservation gotcha). Source: `~/Projects/vscode-skill-curator`. Done — leave it.

## 2. What the extension is
The shipped English Coach extension, rebranded by display-name to "Say It Right". It has TWO halves:
- **Coach (existing, sidebar webview):** rewrite English → idiomatic + Chinese explanation; Translate; read-aloud TTS; clipboard watch; `@coach` chat participant; Review Deck.
- **Pronunciation player (NEW, Plan 1, command-opened editor-tab webview):** per-sentence prosody "stave" (stress ● / reduced · / nuclear word / **intonation arrow** / IPA) + model TTS + 0.25–4× speed + AB-repeat + Shadow loop + Export. Internal command ids `sayItRight.*`, titled **"Say It Right: Practice a Sentence"** / **"Say It Right: Analyze Selection"**.

## 3. DONE in code (all in v0.2.2, committed + pushed, 54 core unit tests pass)
- Plan-1 MVP pronunciation player (see the MVP plan). `core/` is pure TS; `vscode/player/panel.ts` hosts the webview; UI in `media/player/player.{js,css}`.
- Coach provider set trimmed to **Qwen / MiniMax / Gemini / OpenAI** (DeepSeek + MiMo disabled by default) — `package.json` `englishCoach.providerOrder` + `*.enabled` defaults.
- **Qwen JSON-mode fix** (`core/prosody.ts` SYSTEM prompt now contains the literal word "json" — Qwen/DashScope rejects JSON-mode requests whose prompt lacks it; this was the real cause of "no stave / 看不到升降调").
- **Intonation arrow made prominent** — now rendered above each thought-group's nuclear word (`media/player/player.js` `renderStave` + `.tone` CSS).
- `.firecrawl/` research cache excluded from packaging (`.vscodeignore`) + git.

## 4. THE MAIN REMAINING TASK — v0.3.0 (user explicitly wants this)
Build everything below, then republish under a NEW id. The user wants a clean "Say It Right" identity.

**A. Full rebrand → new Marketplace id `vscode-say-it-right`.** Change `package.json` `name`→`vscode-say-it-right`, `displayName`→ **"Say It Right 英语发音教练"** (NOT "…口语教练" — see gotcha §6), the sidebar `viewsContainers` title (currently "English Coach" → "Say It Right"), the `sayItRight.*` command titles, and `description`. (Internal ids `englishCoach.*` for the existing Coach commands/config/views can stay to limit risk — they're invisible except config keys in Settings; a full internal rename is optional follow-up.)

**B. Sidebar "🎙 发音 / Pronunciation" button.** The player is currently command-only (the user couldn't find it → "压根没有这个模块"). Add a button to the Coach sidebar webview (`media/sidebar.js` + `src/vscode/sidebar/provider.ts` HTML) that posts a message → host runs `vscode.commands.executeCommand("sayItRight.practiceSentence")` (or opens the panel directly). This is the #1 UX fix.

**C. Voice + model SELECTION.** User wants to pick TTS **voice** and **model** per provider. Minimal viable = curated `enum` dropdowns in `package.json` settings + good English defaults, read by `vscode/config.ts` `getTtsTarget`/`getAnalysisConfig`. (Richer: in-player/in-sidebar picker — future.) Data to use is in §7.

**D. Two catalog bug-fixes** (`core/models.ts` + manifest defaults):
   - MiniMax default voice is currently the **legacy** `male-qn-qingse` (pre-2.x Chinese voice) → change to an English voice e.g. `English_expressive_narrator`.
   - Remove **`mimo-v2-pro`** and **`mimo-v2-omni`** from the MiMo model list — they auto-route 2026-06-01 and expire 2026-06-30.

**E. Keep the three Chinese models on domestic token-plan endpoints** (they already are: Qwen `dashscope.aliyuncs.com`, MiMo `token-plan-cn.xiaomimimo.com`, MiniMax `api.minimaxi.com`). Prefer Token-Plan-eligible analysis models (e.g. bump Qwen analysis from `qwen3.5-flash` → `qwen3.6-flash`).

## 5. Confirm with the user BEFORE the irreversible publish steps
1. Does 发音 actually render now in 0.2.2? (⌘R reload → ⌘⇧P → "Say It Right: Practice a Sentence" → expect stave + blue intonation arrow over the nuclear word). Build 0.3.0 on a confirmed-working base.
2. New displayName **"Say It Right 英语发音教练"** OK? (or a variant they prefer).
3. They accept English Coach's installs resetting (~1 install) and the URL slug changing to `vscode-say-it-right`.

## 6. CRITICAL Marketplace gotchas — do not relearn these (also in `memory/publishing.md`)
- **Unpublishing reserves BOTH `name` AND `displayName`** — cannot reuse either, even under a different publisher. The rebrand MUST use a new `name` (`vscode-say-it-right`) AND a new `displayName` ("…发音教练", since unpublishing English Coach locks "Say It Right 英语口语教练").
- **A brand-new listing that closely mirrors a live one → auto-rejected "suspicious content"** (duplicate heuristic). This already happened once with `vscode-say-it-right`. So **order matters: unpublish English Coach FIRST, then publish `vscode-say-it-right`.**
- **`VSCE_PAT` can publish to both publishers but could NOT unpublish under `XianweiZhang`** (different Azure DevOps org → TF400813). English Coach is under `Xianwei-Zhang` (the PAT's own publisher) — CLI unpublish *may* work but is untested; if it 403s, the user unpublishes via the web portal (Manage Publishers → extension → ⋮ → Remove).
- **Qwen/DashScope JSON mode requires the literal word "json" in the prompt** (fixed for prosody; keep in mind for any new structured call).
- **Webview cannot access the microphone** (`getUserMedia` blocked) — recording is Plan 3 via host-side ffmpeg (verified feasible: ffmpeg installed + VS Code declares mic entitlement, macOS-first).
- **`vsce package` + `ls -t *.vsix` picks a STALE vsix** — always `rm -f *.vsix` before packaging.
- **Webview assets must live under `media/` not `src/`** (`.vscodeignore` excludes `src/**` → assets would 404 in the packaged VSIX). They're at `media/player/`.

## 7. Verified voice + model research (firecrawl, 2026-05-30) — needed for task C/D
Recommended ENGLISH voices + current models per provider (full lists/sources scraped this session; re-scrape if needed):
- **Qwen** TTS `qwen3-tts-flash` / `qwen3-tts-instruct-flash` (steerable `instructions`). English voices: **Jennifer** (cinematic American F), **Aiden** (American M), **Neil** (news anchor, clear), **Elias** (academic/teaching), Cherry (default), Katerina. Chat: `qwen3.6-flash` (Token Plan), `qwen3.7-max`. Src: help.aliyun.com/zh/model-studio/qwen-tts + /text-generation-model
- **MiniMax** TTS `speech-2.8-turbo` (fast) / `speech-2.8-hd`. 45 `English_*` voices; best: **English_expressive_narrator** (official example), English_CaptivatingStoryteller, English_Trustworth_Man, **English_SereneWoman**, English_WiseScholar. Chat: `MiniMax-M2.7-highspeed` (already fastest). Src: platform.minimax.io/docs/faq/system-voice-id
- **小米 MiMo** TTS `mimo-v2.5-tts`, only 4 English voices: **Chloe** (current), **Mia**, Milo (M), Dean (M). Chat: `mimo-v2.5-pro`, `mimo-v2.5`, `mimo-v2-flash` (drop v2-pro/v2-omni). Src: platform.xiaomimimo.com/docs/en-US/usage-guide/speech-synthesis-v2.5
- **OpenAI** TTS `gpt-4o-mini-tts` (steerable `instructions`), 13 voices; best: **marin/cedar** (recommended), **coral** (steerable teacher). Chat: `gpt-5.4-nano`/`-mini`, `gpt-5.5`. Src: developers.openai.com/api/docs/guides/text-to-speech
- **Gemini** TTS `gemini-2.5-flash-preview-tts` / `gemini-3.1-flash-tts-preview`, 30 voices; best: **Charon** (informative), **Iapetus** (clear), **Sulafat** (warm), Puck (upbeat). Chat: `gemini-3.5-flash`, `gemini-2.5-flash`. Src: ai.google.dev/gemini-api/docs/speech-generation

## 8. Execution recipe for the rebrand (after code is built + tested)
```bash
cd /Users/xianweizhang/Projects/vscode-english-coach
npm run check && npm test           # must be green
rm -f *.vsix
npx @vscode/vsce package --no-dependencies
# verify no junk: unzip -l <vsix> | grep -iE '\.claude|\.firecrawl|extension/src/'  → must be empty
# STEP 1 (irreversible, confirm with user): unpublish the old listing
npx @vscode/vsce unpublish xianwei-zhang.vscode-english-coach --force   # if 403 → user does it in the web portal
# STEP 2: publish the new id (name=vscode-say-it-right, displayName="Say It Right 英语发音教练")
npx @vscode/vsce publish --packagePath vscode-say-it-right-0.3.0.vsix --no-dependencies
# STEP 3: install locally for the user
code --install-extension vscode-say-it-right-0.3.0.vsix --force
git push origin main
```

## 9. Key files / architecture
- Boundary: `src/core/` = pure TS (NO `vscode` import, unit-tested); `src/vscode/` = platform glue; webview never holds secrets (all LLM/TTS in the host).
- Analysis: `core/prosody.ts` (prompt+schema), `core/stave.ts` (→ render rows), `core/segment.ts`, `core/loop.ts`, `core/cache.ts`, `core/providers.ts` (multi-protocol LLM), `core/tts.ts` (incl. `synthesizeOpenAISpeech`), `core/models.ts` (catalogs — edit for task D).
- Player: `src/vscode/player/panel.ts` (WebviewPanel + `getAnalysisConfig`/`getTtsTarget` calls); UI `media/player/player.{js,css}`.
- Coach sidebar (where the 🎙 button goes): `src/vscode/sidebar/provider.ts` + `media/sidebar.js`.
- Config/secrets: `src/vscode/config.ts`, `secrets.ts`, `settings-store.ts`, `audio-cache.ts`.
- Manifest + all settings/commands: `package.json` `contributes`.

## 10. Suggested skills for the next session
- `superpowers:writing-plans` then `superpowers:subagent-driven-development` (or `executing-plans`) to build v0.3.0 task-by-task (that's how Plan 1 was built/reviewed).
- `firecrawl` / `firecrawl-scrape` if any voice/model list needs re-verifying.
- Publishing is manual via `@vscode/vsce` (no skill) — follow §8 + `memory/publishing.md`.
- Do a **memory closeout** at the end (update `say-it-right.md` once 0.3.0 ships under the new id).
